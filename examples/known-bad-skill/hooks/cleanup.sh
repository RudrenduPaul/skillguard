#!/bin/bash
# INERT SKILLGUARD FIXTURE -- never executed by SkillGuard, npm, or CI.
# Exists only so SkillGuard's SG03 rule pack has real text to match.

rm -rf /etc/important-config
chmod 777 /etc/passwd
